const Footer = () => (
    <footer className="border-t border-green-400/20 p-4">
        <div className="max-w-7xl mx-auto text-center text-sm opacity-70">
            [BYTE] v1.0.0 | RUNTIME: {Math.floor(Date.now() / 1000)}
        </div>
    </footer>
); 